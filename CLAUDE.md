# my-blog

마크다운 파일을 읽어서 정적 블로그 웹사이트로 변환하는 프로젝트.

## 목표

- 마크다운(.md)으로 작성한 글을 읽기 좋은 블로그 페이지로 변환
- 깔끔하고 가독성 높은 디자인
- 다크모드 지원
- 모바일 반응형

## 기술 스택 / 제약

- **프레임워크 없음** — 순수 HTML, CSS, JavaScript로만 구현 (React, Vue, Next.js 등 사용 금지)
- 빌드 도구나 번들러 없이 브라우저에서 바로 동작하는 것을 우선한다
- 마크다운 파싱은 외부 라이브러리를 쓸 경우 CDN 스크립트 태그로 가볍게 불러오는 정도만 허용 (예: marked.js). npm/패키지 매니저 도입은 피한다

## 디자인 방향

- 타이포그래피 중심의 깔끔한 레이아웃, 불필요한 장식 지양
- 본문 가독성을 위해 적절한 줄 간격/여백, 읽기 좋은 폭(예: 최대 본문 너비 지정)
- 다크모드: `prefers-color-scheme` 기반 자동 전환 + 수동 토글 버튼 지원
- 모바일: 좁은 화면에서도 레이아웃이 깨지지 않도록 반응형 CSS 적용 (미디어 쿼리 또는 유동적 단위 사용)

## 코딩 컨벤션

- CSS는 커스텀 프로퍼티(CSS 변수)로 라이트/다크 테마 색상을 관리
- JavaScript는 바닐라 JS, 모듈이 필요하면 ES 모듈(`type="module"`) 사용, 별도 트랜스파일 없음
- 파일 구조는 단순하게 유지 (예: `index.html`, `style.css`, `script.js`, `posts/*.md` 등) — 과도한 폴더 분리 지양
